# Agent Notes

- This is a minimal configuration repo containing only `.sessionizer.toml`.
- No build system, package manager, tests, or CI is present.
- Do not invent build/test/lint commands; there is nothing to compile or verify.
